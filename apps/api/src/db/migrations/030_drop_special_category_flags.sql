-- 030_drop_special_category_flags.sql
-- 2026-08-05
--
-- ย้อน migration 029 บางส่วน — user ชี้แจงเพิ่มว่า "วาย"/"ยูริ" (ตอนนี้เปลี่ยนชื่อเป็น BL/GL แล้ว
-- ฝั่งโค้ด) ไม่ควรเป็น boolean column แยกต่างหาก แต่ให้เก็บเป็น string ธรรมดาอยู่ใน works.tags
-- (หมวดหมู่เสริม/free tag array ที่มีอยู่แล้ว) แทน — ใช้ระบบ tag ทั่วไปที่มีอยู่แล้วทั้งชุด (ค้นหา/
-- นับโควต้า/ลบ tag ที่ไม่มีใครใช้) ไม่ต้องสร้างระบบคู่ขนานแยกอีกชุด
--
-- is_yaoi/is_yuri ไม่เคยมีข้อมูลจริงเก็บอยู่เลย (ตั้งไว้ทดสอบเองชั่วคราวแล้ว revert คืนก่อนหน้านี้
-- แล้ว) DROP ตรงๆ ได้เลยไม่มีข้อมูลสูญหาย — categories id 11/12 ยังคงปิดใช้งาน (status=false) ไว้
-- เหมือนเดิมตาม migration 029 (ไม่เกี่ยวข้องกับการเปลี่ยนแปลงรอบนี้)

ALTER TABLE works
  DROP COLUMN IF EXISTS is_yaoi,
  DROP COLUMN IF EXISTS is_yuri;

-- แถวนี้ปิดใช้งานอยู่แล้ว (status=false) ไม่กระทบ UI ไหน — เปลี่ยนชื่อให้ตรงกับที่ใช้จริงตอนนี้
-- (BL) เพื่อความสอดคล้องของข้อมูลเฉยๆ
UPDATE categories SET name = 'BL' WHERE id = 12 AND name = 'วาย';
