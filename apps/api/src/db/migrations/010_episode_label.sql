-- Migration 010: "คำเรียกตอน" (episode label) — เช่น "ตอนที่", "บทที่"
--
-- เก็บเป็น setting ระดับ "เรื่อง" (ไม่ใช่ระดับตอน) เพราะ user ต้องการให้พิมพ์ครั้งเดียว
-- แล้วใช้ซ้ำกับทุกตอนของเรื่องนั้นอัตโนมัติ — เช่น พิมพ์ "ตอนที่" ครั้งแรก ตอนต่อๆ ไป
-- จะเสนอ "ตอนที่" ให้อัตโนมัติ (ไม่ต้องพิมพ์ซ้ำ) เอาไปต่อกับเลขตอนตอน render เช่น "ตอนที่ 5"
-- null = ไม่มีคำนำหน้า แสดงแค่เลขตอนเปล่าๆ
ALTER TABLE works ADD COLUMN IF NOT EXISTS episode_label TEXT;
